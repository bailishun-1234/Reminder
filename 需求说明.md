# 智能语音提醒器 — 需求说明

## 1. 产品概述

一个运行在手机端的提醒工具。用户随时说出待办事项，程序自动记录、智能排期并在指定时间提醒。即使手机处于静音模式，也能通过闹铃响铃唤醒用户。最终打包为手机桌面 APP（通过 PWA 或 H5 壳工程实现）。

## 2. 核心功能

### 2.1 语音添加事项

- 用户以语音输入待办事项，例如："明天下午三点去开会"、"今晚八点给妈妈打电话"、"办港澳通行证"
- 系统自动识别语义，提取**时间**和**事项文本**
- 支持自然语言时间表达：今天/明天/后天、几点几分、上午/下午/晚上、X分钟后/X小时内、本周五、下周一
- 时间识别后以标准化格式展示给用户确认，如有偏差可手动修正

### 2.2 智能排期（自动填充时间）

对于**时间不明确**的事项（如"办港澳通行证"、"买牙膏"），系统不丢弃，而是：

- 自动分配一个**合理的提醒时间**
- 默认排入**今天或明天**的空闲时段，按优先级插空
- 用户可随时修改系统自动分配的时间
- 无时间事项在列表中带有「待排期」标记，与有确定时间的事项区分显示

### 2.3 事项管理与排序

- 所有事项按**提醒时间先后排序**，最近的排最前
- 每个事项展示：提醒时间、事项文本、状态（待提醒/已完成/已过期）
- 支持查看「近期待办」列表（默认显示未来 7 天）
- 支持手动删除已完成或不再需要的事项
- 已完成的事项自动归档，保留记录供查阅

### 2.4 列表操作：置顶、前移、延后

用户可对已添加的事项进行灵活调整：

| 操作 | 说明 |
|------|------|
| **置顶** | 将事项固定到列表最顶部，始终最先提醒 |
| **前移** | 将事项的提醒时间提前（如从明天提前到今天） |
| **延后** | 将事项推迟（如"过两天再说"），轻松一点延后 N 小时/天 |
| **拖拽排序** | 长按拖拽改变事项在列表中的先后顺序 |

- 置顶的事项有醒目的「📌 置顶」标识
- 延后操作提供快捷选项：1小时后、今晚、明天、周末、下周、自定义

### 2.5 延迟提醒 / 暂缓

用户忙碌时可快速暂缓一个提醒：

- 提醒弹窗中提供「过几天再说」选项
- 快捷选择：1小时后、今天晚些时候、明天、本周内、自定义时间
- 延后的事项自动重新排入队列，保持排序正确

### 2.6 智能提醒机制

| 提醒次数 | 行为 | 说明 |
|---------|------|------|
| 第 1 次 | 短提示音 × 1 | 轻柔提醒，适合用户在非睡眠状态 |
| 第 2 次 | 短提示音 × 3 | 间隔 30 秒，加强提醒 |
| 第 3 次 | 持续响铃 | 持续 30 秒，直到用户操作 |

- 每次响铃**同时显示事项文本**，用户一眼看到要做什么
- 相邻两次提醒之间间隔 **30 秒**
- 若用户一直不处理，进入下一轮循环（1声 → 3声 → 持续），间隔逐步缩短
- 闹铃使用 **Web Audio API** 或原生音频接口，确保在静音模式下仍可发出声音

### 2.7 提醒交互

提醒弹出时提供以下操作：

- **🔇 静音本次** — 切断声音，事项保留，下次提醒正常触发
- **✅ 完成事项** — 结束此项目的所有后续提醒，标记为已完成
- **⏰ 延后提醒** — 弹出快捷时间选项（1小时后/今晚/明天/过几天再说…），选后重新排期

### 2.8 页面展示

- 主页面：**待办列表**，按时间排序，最近事项置顶
- 列表项清晰显示：时间 + 事项文本 + 状态标识 + 操作图标（置顶/前移/延后/删除）
- 无时间事项显示「待排期」标签，系统已分配的时间用浅色展示
- 已过期未处理的事项高亮警示
- 已置顶的事项始终保持在列表最上方，带置顶图标
- 页面底部：**语音添加**按钮（麦克风图标）
- 头部显示当前时间，以及下一个待提醒事项倒计时

## 3. 技术方案

| 项目 | 说明 |
|------|------|
| 前端框架 | 使用 Vue 或 React 开发 H5 页面 |
| 语音识别 | 使用 Web Speech API（或集成第三方语音识别服务） |
| 音频播放 | 使用 Web Audio API 实现静音模式响铃 |
| 数据存储 | IndexedDB（结构化存储，支持离线查询） |
| 离线能力 | 通过 Service Worker 缓存，离线可用 |
| 响应式 | 适配主流手机屏幕尺寸 |
| 打包方案 | **PWA（Progressive Web App）**：可添加到手机桌面，全屏运行，支持离线 + 通知 |
| 推送通知 | Service Worker + Notification API（锁屏/后台也能收到提醒） |

### 3.1 PWA 打包为桌面 APP

最终产物为一个 **PWA 应用**：

- 用户通过浏览器访问后，可「添加到主屏幕」生成桌面图标
- 全屏运行，无浏览器地址栏
- 通过 Service Worker 实现离线可用
- 通过推送通知实现在后台/锁屏时弹出提醒
- 无需应用商店审核，更新即时生效

### 3.2 关于静音模式绕过

H5 在静音模式下播放声音的方案：

1. **Web Audio API** 创建一个 `AudioContext`，使用 `OscillatorNode` 生成蜂鸣音，绕过系统静音开关
2. 利用 **Vibration API** 辅助振动提醒（支持设备上）
3. 提醒页面通过 **Notification API** 发送系统通知（需用户授权）
4. **限制**：iOS 上 Web Audio 需用户手势激活；静音模式绕过的可靠性因设备和浏览器而异

## 4. UI 原型描述

### 主页面 — 待办列表

```
 ┌─────────────────────────────┐
 │  ☰ 智能提醒器   10:30       │
 │  下个提醒: 15分钟后          │
 ├─────────────────────────────┤
 │ 📌 置顶                      │
 │   13:00  吃午饭          🔔  │
 │                              │
 │ 📅 今天                      │
 │   14:00  去银行办卡      🔔  │
 │   16:30  取快递          ✅  │
 │   ⏳待定  买牙膏    [自动排] │
 │                              │
 │ 📅 明天                      │
 │   09:30  周会汇报        ⚠️  │
 │   15:00  给妈妈打电话    🔔  │
 │                              │
 │ 📅 后天                      │
 │   10:00  牙医预约        🔔  │
 ├─────────────────────────────┤
 │    [ 🎤 语音添加]  [📋 全部] │
 └─────────────────────────────┘
```

### 提醒弹窗

```
 ┌─────────────────────────────┐
 │  ⏰ 提醒！                   │
 │                              │
 │  去银行办卡                  │
 │  14:00                       │
 │                              │
 │  ┌──────┐ ┌──────┐ ┌─────┐ │
 │  │ 静音  │ │ 延后  │ │完成 │ │
 │  └──────┘ └──────┘ └─────┘ │
 │            ↓                 │
 │    ┌───────────────┐        │
 │    │ 1小时后       │        │
 │    │ 今晚          │        │
 │    │ 明天          │        │
 │    │ 过几天再说…   │        │
 │    └───────────────┘        │
 └─────────────────────────────┘
```

### 语音确认弹窗

```
 ┌─────────────────────────────┐
 │  🎤 识别结果                 │
 │                              │
 │  办港澳通行证                │
 │                              │
 │  ┌─────────────────────┐    │
 │  │ ⏰ 提醒时间          │    │
 │  │ 明天 10:00    [修改] │    │
 │  └─────────────────────┘    │
 │                              │
 │  ┌────────┐  ┌────────────┐ │
 │  │ 取消   │  │ 确认添加 ✅│ │
 │  └────────┘  └────────────┘ │
 └─────────────────────────────┘
```

## 5. 交互流程

```
用户说话 → 语音识别 → 提取时间+事项
                    ┌──┴──┐
              有时间    无时间
                    │   自动分配时间
                    ↓
              展示确认界面
              用户确认/修正时间
                    ↓
              存入待办列表
              按时间排序 + 置顶固定
                    ↓
              ── 用户可随时 ──
              置顶 / 前移 / 延后 / 删除
                    ↓
               ⏳ 等待触发时间
                    ↓
              ┌────┴────┐
              时间到，触发提醒
                    ↓
         ┌──────┼──────────┐
         │ 第1次 │ 第2次    │ 第3次...
         │ 1声   │ 3声      │ 持续响铃
         └──────┴──────────┘
                    ↓
        用户点击「静音」/「完成」/「延后」
```

## 6. 非功能需求

- 提醒准时性误差不超过 5 秒
- 语音识别支持中文普通话
- 页面加载后在前台/后台均能触发提醒（Service Worker + 轮询检查）
- 数据持久化：关闭页面重新打开后数据不丢失
- 浏览器标签页切换或锁屏时，使用 `document.title` 闪烁 + Vibration API + 系统通知辅助提醒
- PWA 性能：首屏加载 < 2 秒，支持添加到桌面后全屏运行

## 7. 后续可扩展

- 云端同步（登录后多设备同步）
- 重复提醒（每天/每周/每月）
- 自定义铃声
- 日历视图
- 分享事项给其他人
- 语音播放待办列表

---

*版本: v0.2 | 日期: 2026-05-17*
