# 智能语音提醒器 — 设计方案

## 1. 技术选型

| 技术 | 选择 | 理由 |
|------|------|------|
| 前端框架 | **Vanilla JS** + Web Components | 零依赖，无构建工具，直接浏览器运行 |
| 样式 | **CSS3** (Grid/Flexbox) + CSS Variables 换肤 | 不依赖任何 UI 库 |
| 语音识别 | **Web Speech API** (`SpeechRecognition`) | 浏览器内置，中文支持良好 |
| 音频 | **Web Audio API** (OscillatorNode) | 绕过静音模式，无需音频文件 |
| 存储 | **IndexedDB** (通过 idb-wrapper 手写) | 比 localStorage 容量大、支持结构化查询 |
| 打包 | 纯静态 PWA (Service Worker 手动编写) | 无 Webpack/Vite，一个 sw.js 搞定 |
| 图标 | **SVG inline** / **Unicode** | 无需图标库 |
| 日期处理 | **手写工具函数** | 场景简单，不需要 moment/dayjs |

**第三方依赖数为 0**。全部使用浏览器标准 API。

## 2. 项目结构

```
reminder/
├── index.html              # 入口 HTML（包含 manifest link + sw 注册）
├── manifest.json           # PWA 清单文件
├── sw.js                   # Service Worker（缓存 + 推送通知）
├── css/
│   └── style.css           # 所有样式
├── js/
│   ├── app.js              # 主入口（协调各模块）
│   ├── db.js               # IndexedDB 封装
│   ├── speech.js           # 语音识别封装
│   ├── audio.js            # Web Audio API 闹铃
│   ├── time.js             # 时间解析 & 格式化工具（自然语言时间提取）
│   ├── reminder.js         # 提醒引擎（定时检查 + 分级提醒）
│   ├── scheduler.js        # 智能排期器（无时间事项自动分配）
│   └── components/
│       ├── todo-list.js     # 待办列表渲染
│       ├── todo-item.js     # 单个事项组件
│       ├── reminder-dialog.js  # 提醒弹窗
│       ├── confirm-dialog.js   # 语音确认弹窗
│       └── mic-button.js    # 语音添加按钮
├── images/
│   └── icons/              # PWA 图标（手工制作的简单 SVG）
└── 需求说明.md
```

## 3. 数据模型

### IndexedDB — `ReminderDB`

**数据库名**: `ReminderDB` | **版本**: 1

#### 对象仓库: `items`

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `string` | 主键，`Date.now() + 随机数` |
| `text` | `string` | 事项文本 |
| `time` | `number \| null` | 提醒时间戳，`null` 表示待排期 |
| `status` | `string` | `pending` 待提醒 / `done` 已完成 / `archived` 已归档 |
| `pinned` | `boolean` | 是否置顶 |
| `remindCount` | `number` | 已提醒次数（用于升级强度） |
| `lastRemindTime` | `number \| null` | 上次提醒时间戳 |
| `snoozeUntil` | `number \| null` | 延后到的时间戳 |
| `createdAt` | `number` | 创建时间戳 |
| `updatedAt` | `number` | 更新时间戳 |
| `sortOrder` | `number` | 同级排序权重（用于拖拽调整） |

**索引**:
- `time` — 按时间排序查询
- `status` — 按状态过滤
- `pinned` — 筛选置顶项

## 4. 模块设计

### 4.1 主入口 `app.js`

```
app.js 职责：
  ├── 页面初始化
  ├── 注册 Service Worker
  ├── 初始化 IndexedDB
  ├── 加载待办数据 → 渲染列表
  ├── 启动提醒引擎（reminder.js）
  ├── 绑定语音按钮（speech.js + mic-button.js）
  └── 绑定全局事件
```

### 4.2 数据层 `db.js`

封装 IndexedDB 操作，对外暴露 Promise 接口：

```js
// 暴露 API
db.getAll()          // 获取所有事项，按时间排序
db.getPending()      // 获取待提醒事项（含置顶优先）
db.add(item)         // 新增事项
db.update(id, data)  // 更新事项（支持局部更新）
db.delete(id)        // 删除事项
db.reorder(id, newSortOrder) // 重排序
```

无需第三方 IDB 库，直接用 `IDBRequest` 包装 Promise。

### 4.3 时间解析 `time.js`

自然语言时间提取，覆盖场景：

```js
parseTime(text)
// 返回: { time: timestamp | null, text: cleanText }
//
// 识别模式（正则匹配）:
//   "明天下午3点开会"     → time=明天15:00, text="开会"
//   "今晚8点打电话"       → time=今天20:00, text="打电话"
//   "后天上午10点半取快递" → time=后天10:30, text="取快递"
//   "5分钟后提醒我关火"   → time=now+5min, text="关火"
//   "下周一开会"          → time=下周一00:00(默认), text="开会"
//   "办港澳通行证"        → time=null, text="办港澳通行证"
//   "买牙膏"             → time=null, text="买牙膏"
```

**匹配优先级**：

1. `(今天|明天|后天|大后天)(早上|上午|中午|下午|晚上|午夜)?(\d+)[:：](\d+)?`
2. `(今|明|后)天(早上|上午|中午|下午|晚上|午夜)`
3. `(\d+)分钟(后|之后)` / `(\d+)小时(后|之后)`
4. `(周[一二三四五六日]|星期[一二三四五六日]|周[1-7])`
5. 以上都不匹配 → `time: null`（无明确时间）

默认时间推断规则：
- 提到"早上/上午" → 默认 09:00
- 提到"中午" → 默认 12:00
- 提到"下午" → 默认 15:00  
- 提到"晚上/今晚" → 默认 20:00
- 仅日期无时间 → 默认 10:00
- 无任何时间词 → time = null

### 4.4 语音识别 `speech.js`

封装 Web Speech API：

```js
recognizeSpeech()
// 返回: Promise<string> 识别文本
//
// 配置:
//   - lang: 'cmn-Hans-CN' (中文普通话)
//   - continuous: false (单次识别)
//   - interimResults: true (实时显示中间结果)
//
// 流程:
//   点击按钮 → 开始监听 → 实时显示中间文字
//   → 用户停止说话(静默1.5s) → 自动结束 → 返回最终文本
//   → 传入 time.parse() + 弹出确认框
```

降级方案：语音识别失败或无权限时，回退到**文本输入框**，用户手动输入。

### 4.5 音频引擎 `audio.js`

使用 Web Audio API 合成蜂鸣音，无需任何音频文件：

```js
playTone(type)
// type: 'single' | 'triple' | 'continuous'
//
// single:    800Hz 方波 × 1 (200ms 时长)
// triple:    800Hz 方波 × 3 (200ms 响 + 150ms 停 × 3)
// continuous: 800Hz 方波 × 持续 30s (或直到用户操作)
//
// 静音绕过原理:
//   AudioContext 不受系统静音键控制
//   前提: 需在用户手势（点击）后创建 AudioContext
```

```js
// 核心代码示意
function createContext() {
  // 惰性创建，在用户首次点击时初始化
  if (!ctx) ctx = new (window.AudioContext || window.webkitAudioContext)();
  return ctx;
}

function beep(duration = 200, frequency = 800, type = 'square') {
  const c = createContext();
  const osc = c.createOscillator();
  const gain = c.createGain();
  osc.type = type;
  osc.frequency.value = frequency;
  gain.gain.value = 0.3;
  osc.connect(gain);
  gain.connect(c.destination);
  osc.start();
  gain.gain.exponentialRampToValueAtTime(0.001, c.currentTime + duration / 1000);
  osc.stop(c.currentTime + duration / 1000);
}
```

### 4.6 提醒引擎 `reminder.js`

核心定时器模块：

```js
// 每秒检查一次
setInterval(checkReminders, 1000);

checkReminders():
  // 1. 从 db 获取所有 pending 状态且 time <= now 的事项
  // 2. 按优先级分级处理：
  //    - 置顶项优先触发
  //    - 普通项按时间顺序触发
  // 3. 根据 remindCount 决定提醒强度：
  //    remindCount === 0 → 'single' (1声)
  //    remindCount === 1 → 'triple' (3声)  
  //    remindCount >= 2  → 'continuous' (持续)
  // 4. 触发提醒时弹出 reminderDialog
  // 5. 用户操作后，更新 remindCount 和 lastRemindTime
  //
  // 节流: 同一事项两次提醒间隔 >= 30秒
```

**提醒间隔策略**：

```
第1次提醒 (remindCount=0) → 播放1声
   └─ 用户未操作 → 等待30秒
第2次提醒 (remindCount=1) → 播放3声
   └─ 用户未操作 → 等待30秒
第3次提醒 (remindCount=2) → 持续30秒响铃
   └─ 用户未操作 → 等待15秒
第4轮起                  → 持续响铃,间隔缩至10秒
```

### 4.7 智能排期器 `scheduler.js`

为无时间事项自动分配时间：

```js
scheduleItem(text)
// 1. 获取今天所有 pending 事项
// 2. 找到今天最晚的提醒时间
// 3. 如果没有同类事项 → 默认排到今天 18:00
// 4. 如果有多个待排期事项 → 按创建顺序间隔 1 小时
// 5. 如果今天已排满 → 推到明天 10:00 起排
//
// 返回: 建议 timestamp
```

无时间事项在列表中显示：
- 时间区展示「⏳ 待排期」
- 浅灰文字显示系统分配的建议时间：「建议: 今天 18:00」
- 点击时间区可手动修改

### 4.8 UI 组件

#### todo-list.js
- 接收数据数组，渲染完整列表
- 按分组渲染：置顶区 → 今天 → 明天 → 本周 → 更远
- 每组带日期标题

#### todo-item.js  
- 渲染单个事项
- 显示：状态图标 + 时间 + 文本 + 操作按钮
- 操作：置顶/取消置顶、前移、延后、完成、删除
- 过期未完成事项红色高亮

#### reminder-dialog.js
- 全屏遮罩弹窗
- 显示：事项文本 + 时间 + 大号闹钟图标
- 三个按钮：静音 / 延后 / 完成
- 延后展开二级选项
- 伴随振动 + 持续响铃

#### confirm-dialog.js
- 语音识别后弹出
- 显示识别文本 + 解析出的时间
- 时间可点击修改（弹出时间选择器）
- 确认 / 取消

#### mic-button.js
- 底部悬浮麦克风按钮
- 点击开始监听，动画（脉冲波纹）
- 识别完成自动收起
- 长按可切换文本输入模式

## 5. 数据流

```
用户语音 → speech.js → rawText
                         ↓
                    time.js → { time, text }
                         ↓
              ┌──── 确认弹窗 ────┐
              │  用户确认/修改时间  │
              └────────┬─────────┘
                       ↓
                  db.add(item)
                       ↓
                  todo-list 重新渲染
                       ↓
              reminder.js 定时检查
                       ↓
              时间到 → audio.js 播放
                   + reminder-dialog 弹窗
                       ↓
              用户操作 → db.update
                   → 关闭弹窗 / 排入下次
```

## 6. PWA 配置

### manifest.json

```json
{
  "name": "智能提醒器",
  "short_name": "提醒器",
  "start_url": "/index.html",
  "display": "standalone",
  "background_color": "#1a1a2e",
  "theme_color": "#16213e",
  "icons": [
    { "src": "/images/icons/icon-192.svg", "sizes": "192x192", "type": "image/svg+xml" },
    { "src": "/images/icons/icon-512.svg", "sizes": "512x512", "type": "image/svg+xml" }
  ]
}
```

### sw.js（Service Worker）

三个职责，由简到繁分阶段实现：

**阶段1 — 静态缓存**：
- install 时预缓存 `index.html`, `css/style.css`, `js/` 下所有 JS
- activate 时清理旧缓存

**阶段2 — 通知推送**：
- 监听 `push` 事件 → 弹出系统通知
- 用户点击通知 → 聚焦页面并打开对应事项

**阶段3 — 后台定时检查**：
- 使用 `sync` 事件或直接轮询
- 锁屏状态下通过 Notification API 提醒

## 7. 样式方案

### 设计风格

- **暗色主题**为主（省电 + 护眼），跟随系统自动切换亮色/暗色
- 纯 CSS 实现，通过 `prefers-color-scheme` 媒体查询
- CSS Variables 统一管理颜色

### 颜色系统

```css
:root {
  --bg-primary: #1a1a2e;
  --bg-secondary: #16213e;
  --bg-card: #0f3460;
  --text-primary: #e8e8e8;
  --text-secondary: #a0a0b0;
  --accent: #e94560;
  --success: #4ecca3;
  --warning: #ffc107;
  --pending: #638bd9;
  --pinned: #ffd700;
}
```

### 响应式

- 移动端适配：宽度 320px ~ 480px
- 使用 `rem` 单位，基准字号 16px
- 列表使用 Flexbox 单列布局
- 弹窗使用 fixed 全屏覆盖

## 8. 提醒强度分级（完整映射）

| remindCount | 音频 | 振动(有Vibration API时) | 弹窗行为 |
|-------------|------|------------------------|----------|
| 0 | 单声 800Hz 200ms | 短振 100ms | 显示+自动3秒后收起 |
| 1 | 三声 (200ms响+150ms停)×3 | 三短振 | 显示+持续 |
| ≥2 | 持续30秒 (800Hz 300ms on/off) | 持续振动 3s | 显示+持续+锁屏通知 |
| ≥6(循环) | 持续+交替频率(800/1000Hz) | 持续振动 | 显示+Flash title闪烁 |

## 9. 实现路线

| 阶段 | 内容 | 产出 |
|------|------|------|
| **P0** | HTML 结构 + CSS 暗色主题 + 待办列表渲染 | 静态页面 |
| **P1** | IndexedDB 读写 + 增删改查 | 数据持久化 |
| **P2** | 语音识别 + 时间解析 + 确认弹窗 | 语音添加闭环 |
| **P3** | 提醒引擎 + Web Audio 闹铃 + 提醒弹窗 | 准时提醒 |
| **P4** | PWA manifest + Service Worker + 桌面图标 | 可安装 APP |
| **P5** | 置顶 / 前移 / 延后 / 拖拽排序 | 列表交互完整 |
| **P6** | 智能排期器（无时间事项自动分配） | 自动排期 |
| **P7** | 亮色主题 + 完善 UI + 动画打磨 | 体验优化 |

## 10. 零依赖的代价与取舍

| 舍弃 | 替代方案 | 影响 |
|------|----------|------|
| Vue/React | Web Components + 手动 DOM | 开发效率略低，但无构建步骤 |
| dayjs/moment | 手写 `time.js`（~50 行） | 功能够用，不支持复杂时区 |
| 图标库 | Unicode 符号 + SVG | 样式有限但够用 |
| UI 框架 | 纯 CSS 手写 | 无组件库便捷度 |
| lodash | 手写 `Array.map/filter/sort` | 原生方法已足够 |
| 构建工具 | 直接在浏览器运行 ES Module | 需要现代浏览器 |

---

*版本: v1.0 | 日期: 2026-05-17*
